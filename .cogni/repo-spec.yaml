# Generated by Node Formation — node identity + governance.
# node_id is the single identity authority (REPO_SPEC_IS_IDENTITY_SSOT);
# scope_id is derived as uuidv5("default", node_id) and must never change.
# See docs/spec/identity-model.md, docs/spec/node-formation.md.

schema_version: "0.1.4"

node_id: "9565871a-cb82-444e-ba18-fed9f6ea3ce1"
scope_id: "32266b2b-6bec-59d4-94f7-cae634bcd9c9"
scope_key: "default"

cogni_dao:
  dao_contract: "0xfbf743cf2857a98c78f6a9b49c05c75fb661caaa"
  plugin_contract: "0x68b0cd790d55b4d7e9534a580642721fa847332f"
  signal_contract: "0xbba9fb2405a5ff0c4c1e464d4c70a9cf352cca48"
  chain_id: "8453"

knowledge:
  database: "knowledge_brother"
  remote:
    provider: dolthub
    owner: "cogni-test-nodes"
    repo: "knowledge-brother"
    url: "https://doltremoteapi.dolthub.com/cogni-test-nodes/knowledge-brother"
    custody: cogni-owned

# Payment rails — activate with: pnpm node:activate-payments
payments:
  status: pending_activation

# Born-reviewable default gates (BORN_REVIEWABLE). No `nodes:` registry → the operator's
# review path resolves this minted repo as a single-node fork and runs these gates against
# the repo-root `.cogni/rules/` shipped by the node-template. Tune per node post-mint.
gates:
  - type: review-limits
    id: review_limits
    with:
      max_changed_files: 50
      max_total_diff_kb: 1500
  - type: ai-rule
    with:
      rule_file: pr-syntropy-coherence.yaml
  - type: ai-rule
    with:
      rule_file: patterns-and-docs.yaml
  - type: ai-rule
    with:
      rule_file: repo-goal-alignment.yaml
